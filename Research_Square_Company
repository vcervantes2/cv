import random
import statistics

random_list = []

for x in range(1,101):
    rand_num = random.randint(1,20)
    random_list.append(rand_num)
    print(rand_num)

mean = statistics.mean(random_list)
stdev = round(statistics.stdev(random_list),2)

print("mean: ", mean)
print("stdev: ", stdev)
